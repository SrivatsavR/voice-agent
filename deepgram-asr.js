// Updated authorization header format for Deepgram API calls
const apiKey = 'your_api_key_here'; // Replace with actual API key

const headers = {
    'Authorization': `Token ${apiKey}`, // This line needs to be updated
    'Content-Type': 'application/json',
};

// Update following line with the correct format
// 'Authorization': `Token ${apiKey}` -> 'Authorization': `Token ${apiKey}`
// This is a placeholder comment to guide further updates